# public/css/pages

Page-specific CSS for the Project Purple static app shell.

Use this folder only for styles that are unique to one page.

Shared components, layout systems, cards, buttons, navigation, and theme styles belong in `public/css/core/`.
